# OAuth client ID (same as frontend)
GOOGLE_CLIENT_ID=

# OAuth client secret — NEVER expose this in frontend code or logs
GOOGLE_CLIENT_SECRET=

# Exact redirect URI registered in Google Cloud Console
# For @react-oauth/google authorization-code flow (popup), use: postmessage
GOOGLE_REDIRECT_URI=

# Port the Express server listens on
PORT=4000

# Google OAuth client ID exposed to Vite bundler (public — no secret here)
VITE_GOOGLE_CLIENT_ID=

# Base URL of the Express backend (no trailing slash)
# Example: http://localhost:4000
VITE_BACKEND_URL=
